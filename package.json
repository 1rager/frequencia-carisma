{
  "name": "frequencia-carisma",
  "version": "1.0.0",
  "scripts": {
    "install": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "node backend/index.js"
  }
}
